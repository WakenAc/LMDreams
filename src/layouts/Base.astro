---
import BaseLayout from '@/layouts/BaseLayout.astro';

interface Props {
  currentPath?: string;
  [key: string]: unknown;
}

const { currentPath = '/', ...rest } = Astro.props as Props;
---

<BaseLayout currentPath={currentPath} {...rest}>
  <slot />
</BaseLayout>
